#!/bin/sh
set -e

if [ "$1" = configure ]; then
  # Add a user for all NDN related files and processes
  adduser \
      --system \
      --quiet \
      --home /var/lib/ndn \
      --disabled-password \
      --group \
      ndn-user
fi

#DEBHELPER#

exit 0
